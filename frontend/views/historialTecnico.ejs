<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Historial - Tecnico</title>
    <link rel="stylesheet" href="/css/estilos.css">
</head>
<body class="app-body">

<aside class="sidebar-pro">
    <div class="brand">
        <div class="brand-icon">IT</div>
        <div>
            <h2>Incidencias TI</h2>
            <span>Panel Tecnico</span>
        </div>
    </div>

    <nav>
        <a href="/dashboard">Dashboard</a>
        <a href="/incidencias-tecnico">Incidencias</a>
        <a class="active" href="/historial-tecnico">Historial</a>
        <a href="/">Cerrar sesion</a>
    </nav>
</aside>

<main class="content-pro">

    <header class="topbar-pro">
        <div>
            <h1>Historial de Incidencias</h1>
            <p>Historial completo de incidencias atendidas en el sistema</p>
        </div>
    </header>

    <section class="table-pro">

        <h3>Todas las incidencias</h3>

        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Titulo</th>
                    <th>Solicitante</th>
                    <th>Prioridad</th>
                    <th>Estado</th>
                    <th>Accion</th>
                </tr>
            </thead>

            <tbody>

                <% if (incidencias.length === 0) { %>

                    <tr>
                        <td colspan="6">
                            No hay incidencias registradas.
                        </td>
                    </tr>

                <% } else { %>

                    <% incidencias.forEach((incidencia) => { %>

                    <tr>

                        <td>
                            #<%= incidencia.CI_IdIncidencia %>
                        </td>

                        <td>
                            <%= incidencia.CT_Titulo %>
                        </td>

                        <td>
                            <%= incidencia.nombreSolicitante %>
                        </td>

                        <td>
                            <span class="badge prioridad-<%= incidencia.CT_Prioridad.toLowerCase() %>">
                                <%= incidencia.CT_Prioridad %>
                            </span>
                        </td>

                        <td>
                            <span class="badge estado-<%= incidencia.CT_Estado.toLowerCase().replace(' ', '-') %>">
                                <%= incidencia.CT_Estado %>
                            </span>
                        </td>

                        <td>

                            <button
                                class="btn-primary"
                                onclick="window.location.href='/gestionar-incidencia/<%= incidencia.CI_IdIncidencia %>'">

                                Ver Detalle

                            </button>

                        </td>

                    </tr>

                    <% }) %>

                <% } %>

            </tbody>

        </table>

    </section>

</main>

</body>
</html>